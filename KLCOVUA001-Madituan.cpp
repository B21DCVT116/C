#include <bits/stdc++.h>
using namespace std;


int main(){
	int t;
	cin >> t;
	while(t--){
		int n,k,x,y;
		cin >> n >> k >> x >> y;
		int banco[n][n] dem=1;
		memset (banco, 0 ,sizeof(banco));
		banco[x][y]=1;
		while
	}
}
